using System;

class Program
{
    static void Main(string[] args)
    { /*Вывести на экран, консольного приложения, прямоугольный треугольник 
    из звездочек “*”, с длиной катета m.*/
        Console.WriteLine("Введите длину катета = ");
        int m = Convert.ToInt32(Console.ReadLine());

        for (int i = 0; i <= m; i++)
        {
            int k = 0;
            while (k < i)
            {
                Console.Write("*");
                k++;
            }
            Console.WriteLine();
        }
    }
}
